# Normalize line endings: text files commit as LF, auto-detected.
* text=auto eol=lf

# Treat these as binary (no eol conversion, no diff noise) if ever added.
*.png binary
*.jpg binary
*.pdf binary
*.ico binary
